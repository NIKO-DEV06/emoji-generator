import { TranformResponseInterceptor } from './tranform-response.interceptor';

describe('TranformResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new TranformResponseInterceptor()).toBeDefined();
  });
});
